<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Digital Art | CHRIS</title>
  <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@300;400;600&display=swap" rel="stylesheet">
  
  <style>
    /* ================= 1. CORE STYLES (Same as Home) ================= */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    
    body {
      font-family: 'Raleway', sans-serif;
      background: #ffffff;
      color: #111;
      overflow-x: hidden;
      opacity: 0;
      animation: fadeInPage 0.8s ease forwards;
    }

    @keyframes fadeInPage { to { opacity: 1; } }

    #dust-canvas {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      pointer-events: none;
      z-index: 5;
    }

    /* NAV */
    .nav {
      position: fixed; top: 0; width: 100%; z-index: 1000;
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(0,0,0,0.05);
    }
    .nav-inner {
      max-width: 1200px; margin: 0 auto; padding: 20px 40px;
      display: flex; justify-content: space-between; align-items: center;
    }
    .nav-logo { font-weight: 600; font-size: 20px; letter-spacing: 1px; }
    .nav-links { list-style: none; display: flex; gap: 30px; }
    .nav-links a { text-decoration: none; color: #111; font-size: 13px; text-transform: uppercase; opacity: 0.6; transition: 0.3s; }
    .nav-links a:hover, .nav-links a.active { opacity: 1; font-weight: 600; }

    /* PAGE HEADER */
    .page-header {
      padding: 150px 20px 50px;
      text-align: center;
      position: relative;
      z-index: 10;
    }
    .page-header h1 { font-size: 3rem; text-transform: uppercase; letter-spacing: 5px; opacity: 0; animation: fadeUp 1s ease forwards; }
    .page-header p { font-size: 1rem; color: #666; margin-top: 10px; opacity: 0; animation: fadeUp 1s ease forwards 0.2s; }

    /* ================= 2. PORTFOLIO GRID ================= */
    .portfolio-section {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px 100px;
      position: relative;
      z-index: 10;
    }

    .art-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
      gap: 30px;
    }

    .art-card {
      position: relative;
      overflow: hidden;
      background: #f9f9f9;
      aspect-ratio: 4 / 5; /* Keeps all cards the same height */
      transition: transform 0.5s cubic-bezier(0.2, 1, 0.3, 1);
    }

    .art-card:hover {
      transform: translateY(-10px);
    }

    .art-card img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.8s ease;
    }

    .art-card:hover img {
      transform: scale(1.05);
    }

    .art-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(to top, rgba(0,0,0,0.7), transparent);
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      padding: 30px;
      opacity: 0;
      transition: opacity 0.4s ease;
      color: white;
    }

    .art-card:hover .art-overlay { opacity: 1; }
    .art-overlay h3 { font-size: 1.2rem; margin-bottom: 5px; }
    .art-overlay p { font-size: 0.8rem; text-transform: uppercase; letter-spacing: 1px; }

    /* ANIMATION */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .reveal { opacity: 0; transform: translateY(30px); transition: 1s ease; }
    .reveal.active { opacity: 1; transform: translateY(0); }
    footer { padding: 50px; text-align: center; font-size: 12px; color: #999; }

    /* MOBILE ADJUSTMENTS */
    @media (max-width: 768px) {
      .nav-inner { padding: 15px 20px; }
      .nav-links a { font-size: 10px; gap: 10px; }
      .page-header h1 { font-size: 2rem; }
      .art-grid { grid-template-columns: 1fr; } /* Stack images on mobile */
    }
  </style>
</head>

<body>
  <canvas id="dust-canvas"></canvas>

  <nav class="nav">
    <div class="nav-inner">
      <div class="nav-logo">CHRIS</div>
      <ul class="nav-links">
        <li><a href="index.html">Home</a></li>
        <li><a href="digital-art.html" class="active">Art</a></li>
        <li><a href="graphic-design.html">Design</a></li>
        <li><a href="logos.html">Logos</a></li>
        <li><a href="photo-edits.html">Edits</a></li>
      </ul>
    </div>
  </nav>

  <header class="page-header">
    <h1>Digital Art</h1>
    <p>Conceptual illustrations and character designs.</p>
  </header>

  <section class="portfolio-section">
    <div class="art-grid">
      
      <div class="art-card reveal">
        <img src="images/art1.jpg" alt="Artwork 1">
        <div class="art-overlay">
          <h3>Project Title One</h3>
          <p>Concept Illustration</p>
        </div>
      </div>

      <div class="art-card reveal">
        <img src="images/art2.jpg" alt="Artwork 2">
        <div class="art-overlay">
          <h3>Project Title Two</h3>
          <p>Character Design</p>
        </div>
      </div>

      <div class="art-card reveal">
        <img src="images/art3.jpg" alt="Artwork 3">
        <div class="art-overlay">
          <h3>Project Title Three</h3>
          <p>Digital Painting</p>
        </div>
      </div>

      </div>
  </section>

  <footer>© 2025 EMMY CHRIS</footer>

  <script>
    // 1. Reveal Logic
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) entry.target.classList.add('active');
      });
    }, { threshold: 0.1 });
    document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

    // 2. Dust Physics (Same as Home)
    const canvas = document.getElementById("dust-canvas");
    const ctx = canvas.getContext("2d");
    let particles = [];

    function resize() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }
    window.addEventListener("resize", resize);
    resize();

    const mouse = { x: -1000, y: -1000 };
    const updateMouse = (x, y) => { mouse.x = x; mouse.y = y; };
    window.addEventListener("mousemove", (e) => updateMouse(e.clientX, e.clientY));
    window.addEventListener("touchmove", (e) => updateMouse(e.touches[0].clientX, e.touches[0].clientY));
    window.addEventListener("touchstart", (e) => updateMouse(e.touches[0].clientX, e.touches[0].clientY));

    class Particle {
      constructor() {
        this.reset();
        this.y = Math.random() * canvas.height;
      }
      reset() {
        this.x = Math.random() * canvas.width;
        this.groundY = canvas.height - (Math.random() * 25);
        this.y = this.groundY;
        this.size = Math.random() * 2 + 1;
        this.vx = (Math.random() - 0.5) * 0.8;
        this.vy = 0;
        this.alpha = Math.random() * 0.4 + 0.2;
      }
      update() {
        const dx = mouse.x - this.x;
        const dy = mouse.y - this.y;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if (dist < 100) {
          const force = (100 - dist) / 100;
          this.vy -= force * 2; 
          this.vx += (dx / dist) * force * 5;
        }
        this.x += this.vx; this.y += this.vy;
        this.vy += 0.15; this.vx *= 0.96; 
        if (this.y > this.groundY) { this.y = this.groundY; this.vy = 0; }
      }
      draw() {
        ctx.fillStyle = `rgba(50, 50, 50, ${this.alpha})`;
        ctx.beginPath(); ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2); ctx.fill();
      }
    }

    for (let i = 0; i < 120; i++) particles.push(new Particle());
    function animate() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      particles.forEach(p => { p.update(); p.draw(); });
      requestAnimationFrame(animate);
    }
    animate();
  </script>
</body>
</html>