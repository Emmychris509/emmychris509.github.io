<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CHRIS | Brand Designer & Concept Artist</title>
  <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@300;400;600&display=swap" rel="stylesheet">
  
  <style>
    /* ================= 1. CORE & DESKTOP STYLES ================= */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    
    body {
      font-family: 'Raleway', sans-serif;
      background: #ffffff;
      color: #111;
      overflow-x: hidden;
      opacity: 0;
      animation: fadeInPage 0.8s ease forwards;
    }

    @keyframes fadeInPage { to { opacity: 1; } }

    #dust-canvas {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      pointer-events: none;
      z-index: 5;
    }

    /* NAV */
    .nav {
      position: fixed; top: 0; width: 100%; z-index: 1000;
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(0,0,0,0.05);
    }
    .nav-inner {
      max-width: 1200px; margin: 0 auto; padding: 20px 40px;
      display: flex; justify-content: space-between; align-items: center;
    }
    .nav-logo { font-weight: 600; font-size: 20px; letter-spacing: 1px; }
    .nav-links { list-style: none; display: flex; gap: 30px; }
    .nav-links a { text-decoration: none; color: #111; font-size: 13px; text-transform: uppercase; opacity: 0.6; transition: 0.3s; }
    .nav-links a:hover, .nav-links a.active { opacity: 1; font-weight: 600; }

    /* HERO - Adjust Image Here */
    .hero {
      height: 100vh; /* Change to 90vh if you want to see the 'About' section sooner */
      position: relative;
      display: flex;
      align-items: center;
      justify-content: flex-end; 
    }
    .hero img {
      position: absolute; inset: 0;
      width: 100%; height: 100%;
      z-index: 0;
      
      /* ADJUST IMAGE POSITION HERE */
      object-fit: cover; 
      object-position: left center; /* 'left' ensures your character is visible */
    }
    .hero-content {
      position: relative;
      z-index: 10;
      margin-right: 10%; 
      text-align: right;
      
      /* ANIMATION RESTORED */
      opacity: 0;
      animation: fadeUp 1.2s ease forwards;
      animation-delay: 0.3s;
    }
    .hero h1 { font-size: 4.5rem; line-height: 1; margin-bottom: 10px; }
    .hero p { font-size: 1.1rem; letter-spacing: 2px; text-transform: uppercase; color: #555; }

    /* ANIMATION KEYFRAMES */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .section { position: relative; z-index: 10; max-width: 900px; margin: auto; padding: 120px 20px; text-align: center; }
    .reveal { opacity: 0; transform: translateY(30px); transition: 1s ease; }
    .reveal.active { opacity: 1; transform: translateY(0); }
    footer { padding: 50px; text-align: center; font-size: 12px; color: #999; }

    /* ================= 2. MOBILE-ONLY ================= */
    @media (max-width: 768px) {
      .nav-inner { padding: 15px 20px; }
      .nav-links { gap: 12px; }
      .nav-links a { font-size: 10px; }

      .hero { justify-content: center; height: 85vh; }
      .hero img { object-position: 25% center; } /* Move image for better mobile focus */
      .hero-content { 
        margin-right: 0; text-align: center; 
        background: rgba(255,255,255,0.4); padding: 20px;
        width: 85%; backdrop-filter: blur(4px);
      }
      .hero h1 { font-size: 2.5rem; }
      .hero p { font-size: 0.8rem; }
    }
  </style>
</head>

<body>
  <canvas id="dust-canvas"></canvas>

  <nav class="nav">
    <div class="nav-inner">
      <div class="nav-logo">CHRIS</div>
      <ul class="nav-links">
        <li><a href="index.html" class="active">Home</a></li>
        <li><a href="digital-art.html">Art</a></li>
        <li><a href="graphic-design.html">Design</a></li>
        <li><a href="logos.html">Logos</a></li>
        <li><a href="photo-edits.html">Edits</a></li>
      </ul>
    </div>
  </nav>

  <section class="hero">
    <img src="images/hero.jpg" alt="Hero"> 
    <div class="hero-content">
      <h1>Emmy Chris</h1>
      <p>Brand Designer & Concept Artist</p>
    </div>
  </section>

  <section class="section reveal">
    <h2>About</h2>
    <p>I design visual identities and conceptual systems that communicate clearly. Swipe or move your mouse along the bottom to see the dust kick up.</p>
  </section>

  <footer>© 2025 EMMY CHRIS</footer>

  <script>
    // 1. Reveal Logic
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) entry.target.classList.add('active');
      });
    }, { threshold: 0.1 });
    document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

    // 2. Dust Physics (Mouse + Touch)
    const canvas = document.getElementById("dust-canvas");
    const ctx = canvas.getContext("2d");
    let particles = [];

    function resize() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }
    window.addEventListener("resize", resize);
    resize();

    const mouse = { x: -1000, y: -1000 };
    const updateMouse = (x, y) => { mouse.x = x; mouse.y = y; };
    window.addEventListener("mousemove", (e) => updateMouse(e.clientX, e.clientY));
    window.addEventListener("touchmove", (e) => updateMouse(e.touches[0].clientX, e.touches[0].clientY));
    window.addEventListener("touchstart", (e) => updateMouse(e.touches[0].clientX, e.touches[0].clientY));

    class Particle {
      constructor() {
        this.reset();
        this.y = Math.random() * canvas.height;
      }
      reset() {
        this.x = Math.random() * canvas.width;
        this.groundY = canvas.height - (Math.random() * 25);
        this.y = this.groundY;
        this.size = Math.random() * 2 + 1;
        this.vx = (Math.random() - 0.5) * 0.8;
        this.vy = 0;
        this.alpha = Math.random() * 0.4 + 0.2;
      }
      update() {
        const dx = mouse.x - this.x;
        const dy = mouse.y - this.y;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if (dist < 100) {
          const force = (100 - dist) / 100;
          this.vy -= force * 2; 
          this.vx += (dx / dist) * force * 5;
        }
        this.x += this.vx; this.y += this.vy;
        this.vy += 0.15; this.vx *= 0.96; 
        if (this.y > this.groundY) { this.y = this.groundY; this.vy = 0; }
      }
      draw() {
        ctx.fillStyle = `rgba(50, 50, 50, ${this.alpha})`;
        ctx.beginPath(); ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2); ctx.fill();
      }
    }

    for (let i = 0; i < 120; i++) particles.push(new Particle());
    function animate() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      particles.forEach(p => { p.update(); p.draw(); });
      requestAnimationFrame(animate);
    }
    animate();
  </script>
</body>
</html>