/* RESET & BASE */
* { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }
body {
  font-family: 'Raleway', Arial, sans-serif;
  background: #fff;
  color: #111;
  padding-top: 90px;
  overflow-x: hidden;
}

/* DUST CANVAS */
#dust-canvas {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 99;
}

/* NAVIGATION */
.nav {
  position: fixed;
  top: 0;
  width: 100%;
  z-index: 1000;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(10px);
}
.nav-inner {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px 40px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.nav-logo { font-weight: 600; font-size: 18px; }
.nav-links { list-style: none; display: flex; gap: 30px; }
.nav-links a { text-decoration: none; font-size: 14px; color: #111; opacity: 0.7; transition: 0.3s; }
.nav-links a:hover, .nav-links a.active { opacity: 1; border-bottom: 1px solid #111; }

/* HERO - ALIGNED RIGHT */
.hero {
  height: 100vh;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: flex-end; /* Pushes content to the right */
}
.hero img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.hero-content {
  position: relative;
  z-index: 1;
  max-width: 600px;
  margin-right: 10%; /* Space from the right edge */
  text-align: right;
  animation: fadeUp 1.2s ease forwards;
}
.hero h1 { font-size: 4rem; letter-spacing: -1px; }
.hero p { font-size: 1.2rem; margin-top: 10px; }

/* PORTFOLIO GRID (For Sub-Pages) */
.portfolio-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 30px;
  margin-top: 40px;
}
.art-card {
  background: #fdfdfd;
  border: 1px solid #eee;
  border-radius: 12px;
  overflow: hidden;
  transition: transform 0.4s ease;
}
.art-card:hover { transform: translateY(-10px); }
.art-card img { width: 100%; height: 350px; object-fit: cover; }
.card-info { padding: 20px; }
.card-info h3 { margin-bottom: 5px; font-size: 1.2rem; }
.card-info p { font-size: 0.9rem; color: #666; margin-bottom: 15px; }
.view-link { 
  display: inline-block;
  text-decoration: none; 
  color: #111; 
  font-weight: 600; 
  font-size: 0.8rem;
  border-bottom: 1px solid #111;
}

/* ANIMATIONS & REVEALS */
.section { max-width: 1100px; margin: auto; padding: 120px 20px; }
.reveal { opacity: 0; transform: translateY(30px); transition: 0.8s ease; }
.reveal.active { opacity: 1; transform: translateY(0); }

@keyframes fadeUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

/* CONTACT */
.contact-links { display: flex; gap: 20px; margin-top: 20px; }
.contact-links a { padding: 12px 20px; border: 1px solid #111; border-radius: 30px; text-decoration: none; color: #111; transition: 0.3s; }
.contact-links a:hover { background: #111; color: #fff; }

/* CONTACT RECTANGLES */
.contact-grid {
  display: flex;
  justify-content: center;
  gap: 20px;
  flex-wrap: wrap;
  margin-top: 40px;
}
.contact-rect {
  text-decoration: none;
  color: #111;
  border: 1px solid #111;
  padding: 15px 40px;
  min-width: 150px;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 2px;
  transition: all 0.4s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
}
/* Specific Brand Hovers */

/* WhatsApp Green */
.btn-whatsapp:hover {
  background: #25D366 !important;
  color: #fff !important;
  border-color: #25D366 !important;
  transform: translateY(-5px);
  box-shadow: 0 10px 20px rgba(37, 211, 102, 0.3);
}

/* Email - Google Multi-color Strip */
.btn-email:hover {
  /* This creates a 4-color gradient strip at the bottom of the button */
  border-bottom: 4px solid #4285F4 !important; 
  background: #f8f8f8 !important;
  color: #111 !important;
  transform: translateY(-5px);
  /* Optional: A multi-color glow */
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.05);
  position: relative;
}

/* Instagram Gradient */
.btn-instagram:hover {
  background: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%) !important;
  color: #fff !important;
  border-color: transparent !important;
  transform: translateY(-5px);
  box-shadow: 0 10px 20px rgba(220, 39, 67, 0.3);
}

/* BACK TO TOP BUTTON */
.back-to-top {
  position: fixed;
  bottom: 30px; 
  right: 30px;
  width: 40px; 
  height: 40px;
  background: #111; 
  color: #fff;
  border: none; 
  cursor: pointer;
  display: flex; 
  align-items: center; 
  justify-content: center;
  font-size: 18px;
  opacity: 0; 
  visibility: hidden;
  transition: all 0.4s ease;
  z-index: 2000;
}
.back-to-top.active { 
  opacity: 1; 
  visibility: visible; 
}
.back-to-top:hover {
  background: #333;
}

/* MOBILE TWEAK */
@media (max-width: 600px) {
  .contact-grid { flex-direction: column; } /* Added this to stack buttons nicely on phones */
  .contact-rect { width: 100%; margin-bottom: 10px; }
}

/* === PASTE NEW CODE END === */

footer { text-align: center; padding: 40px; color: #666; }